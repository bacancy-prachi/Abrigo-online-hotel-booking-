<%= form_with(model: [@hotel, @room], local: true) do |form| %>
<% if room.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

  <ul>
    <% room.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
<%= form.hidden_field :hotel_id, value: @room.hotel_id %>
<div class="field">
  <%= form.label :room_number %>
  <%= form.text_field :room_number %>
</div>

<div class="field">
  <%= form.label :availibility %>
  <%= form.check_box :availibility,{},"true","false" %>
</div>

<div class="field">
  <%= form.collection_select :category_id, Category.all,:id,:name, include_blank: false %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>

<%= form_with(model: room, local: true) do |form| %>
<% if room.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

  <ul>
    <% room.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

  <%# <div class="field">
    <%= form.label :availibility %>
    <%# <%= form.check_box :availibility %>
    <%# </div> %>

    <div class="field">
      <%= form.label :number_of_room %>
      <%= form.text_field :room_id %>
    </div>

    <div class="field">
      <%= form.collection_select :category_ids, Category.all,:id,:name, include_blank: false %>

    </div>
    
    <div class="actions">
      <%= form.submit %>
    </div>
    <% end %>
